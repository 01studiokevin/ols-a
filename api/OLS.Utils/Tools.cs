using Microsoft.AspNetCore.Mvc;
using System;
namespace OLS.Utils
{
    public class Tools
    {
        /*public string JsonStringToJsonStandard(string str)
        {
            var jsonResult = new JsonResult();
            try
            {
                jsonResult.Data = jsonstr;
                jsonResult.ContentType = "html/text";
                Object obj = Newtonsoft.Json.JsonConvert.DeserializeObject(jsonResult.Data.ToString());
                jsonstr = Newtonsoft.Json.JsonConvert.SerializeObject(obj);
            }
            catch (Exception)
            {
                jsonstr = null;
            }
            return str;
        }*/
    }
}
